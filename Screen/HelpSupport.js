import React from 'react';

import {Text, View} from 'react-native';

const Help = ({navigation}) => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: '#262626'}}>Help and Support !</Text>
    </View>
  );
};

export default Help;
